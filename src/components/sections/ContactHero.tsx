import { PageHero } from "@/components/shared/PageHero";

export function ContactHero() {
  return (
    <PageHero
      eyebrow="Get In Touch"
      title="Visit, call, or send us a message."
      description="For appointments, use our booking flow. For everything else, we're one message away."
      tone="teal"
      glow="right"
      compact
    />
  );
}
