import { PageHero } from "@/components/shared/PageHero";

export function DoctorsDirectoryHero() {
  return (
    <PageHero
      eyebrow="Our Medical Team"
      title="Every doctor here chose one discipline and mastered it."
      description="Search by specialization to find the right doctor for your pet's condition, or browse the full team below."
      tone="teal"
      glow="right"
    />
  );
}
