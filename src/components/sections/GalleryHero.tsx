import { PageHero } from "@/components/shared/PageHero";

export function GalleryHero() {
  return (
    <PageHero
      eyebrow="Radical Operational Transparency"
      title="The hospital, exactly as your pet will experience it."
      description="Every room you'll walk into, every table your pet will lie on — no stock photography, no surprises on arrival."
      tone="green"
      glow="right"
    />
  );
}
